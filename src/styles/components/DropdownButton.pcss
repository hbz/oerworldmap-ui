@import '../colors.pcss';
@import '../variables.pcss';

.DropdownButton {
  display: inline-block;

  & > i {
    position: relative;
    font-size: 1.1rem;
  }

  @media print {
    display: none;
  }
}
